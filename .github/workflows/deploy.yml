name: "Despliegue Infraestructura UCE"

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  terraform:
    name: "Terraform"
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Instalar Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.0

      - name: Terraform Init
        id: init
        run: terraform init
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_SESSION_TOKEN: ${{ secrets.AWS_SESSION_TOKEN }}
          AWS_DEFAULT_REGION: "us-east-1"

      - name: Terraform Validate
        id: validate
        run: terraform validate

      - name: Terraform Plan
        id: plan
        run: terraform plan -no-color
        continue-on-error: true
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_SESSION_TOKEN: ${{ secrets.AWS_SESSION_TOKEN }}
          AWS_DEFAULT_REGION: "us-east-1"
          # OJO: Asegúrate de que esta línea tenga tu llave PÚBLICA completa, no cortada con puntos suspensivos
          TF_VAR_public_key_content: "ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABgQC..." 

      # --- AQUÍ EMPIEZA LO NUEVO QUE NECESITABAS ---

      - name: Terraform Apply
        id: apply
        # El -auto-approve es vital para que no se quede esperando un "yes" manual
        run: terraform apply -auto-approve
        env:
          # Es necesario REPETIR las credenciales aquí porque es un paso nuevo
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_SESSION_TOKEN: ${{ secrets.AWS_SESSION_TOKEN }}
          AWS_DEFAULT_REGION: "us-east-1"
          # Usa la misma llave que pusiste arriba
          TF_VAR_public_key_content: "ssh-rsa  dgtcom@DgTcOm
" 


          - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_SESSION_TOKEN: ${{ secrets.AWS_SESSION_TOKEN }}
          AWS_DEFAULT_REGION: "us-east-1"
          # IMPORTANTE: Aquí debes poner tu llave pública REAL, no la variable vacía
          TF_VAR_public_key_content: "ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABgQC..." 

      - name: Mostrar URL del Balanceador
        run: |
          echo "---------------------------------------------"
          echo "🌐 URL DE TU APLICACIÓN:"
          terraform output -raw url_balanceador
          echo "---------------------------------------------"